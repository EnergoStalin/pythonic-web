[project]
name = "pythonic-web"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "openapi-generator-cli>=7.20.0",
]

[tool.uv.workspace]
members = [
    "services/auth",
]

[tool.isort]
profile = "black"
